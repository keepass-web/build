{
  "name": "@keepass-web/build-tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pinned build toolchain for the keepass-web project build container.",
  "scripts": {
    "typecheck": "tsc --noEmit -p inliner/tsconfig.json",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "node --experimental-strip-types --test inliner/tests/build.test.ts"
  },
  "dependencies": {
    "@biomejs/biome": "2.5.0",
    "typescript": "6.0.3"
  }
}
